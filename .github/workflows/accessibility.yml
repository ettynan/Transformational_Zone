name: Accessibility check

on:
  push:
    branches: [main]
  pull_request:

jobs:
  lighthouse-accessibility:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm ci
        working-directory: Transformational_Zone

      - name: Build site
        run: npm run build
        working-directory: Transformational_Zone

      - name: Run Lighthouse accessibility audit
        uses: treosh/lighthouse-ci-action@v12
        with:
          # Path updated to .cjs to match the renamed file
          configPath: "Transformational_Zone/.lighthouserc.cjs"
          workingDirectory: "Transformational_Zone"